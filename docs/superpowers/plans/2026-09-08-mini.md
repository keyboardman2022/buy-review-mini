# 微信小程序 Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for the independent client and SMS tasks; root implements and integrates the server. Do not spawn nested reviewers.

**Goal:** 四项用户需求的本地可运行微信小程序与持久化后端。
**Architecture:** 原生客户端通过REST调用单体Node后端，SQLite事务执行权限和审批规则，持久outbox发短信。
**Tech Stack:** Node24 built-in HTTP/SQLite/crypto，WXML/WXSS/JS。
**Spec:** docs/spec.md。统一接口：docs/api.md。

## Global Constraints
不修改旧buy-review网页；无真实短信、无发布；仅后端判权限；原生小程序无需npm构建。演示行为必须有明确开关。

## Task 1: HTTP业务后端（root）
- [ ] tests/domain.test.mjs：以3票中2接受为通过、2票平票为拒绝、veto提前拒绝、unanimous等待全员等独立常量测试，先验证缺失失败。
- [ ] server/domain.mjs：实现evaluate(rule,decisions)、验证器；server/db.mjs：SQLite模式与事务；server/service.mjs：用户、关系、商品、投票业务；server/http.mjs：API、上传和响应；server/index.mjs：本地启动。
- [ ] tests/api.test.mjs：真实HTTP+临时SQLite，注册三个账号，申请/接受好友，创建商品，评论与赞踩，发起/投票/读回结论，未授权403，重复409，重开数据库仍有记录。
- [ ] 运行node --test tests/domain.test.mjs tests/api.test.mjs，修复失败，再验证。

## Task 2: 原生小程序客户端（独立实现）
- [ ] 按docs/api.md实现miniprogram/全部页面、project.config.json；薄API封装统一处理失效会话。
- [ ] tests/client.test.mjs通过vm加载页面逻辑和受控wx边界，验证元转分、必填评论、选人、API错误、重复提交保护。
- [ ] scripts/check-mini.mjs检查页面引用和JS语法；开发者工具只open/编译，本次不upload。

## Task 3: 短信适配（独立实现）
- [ ] server/sms.mjs导出createSmsProvider(config) -> {mode,send({kind,phone,params,body})}；mock只返回mock；tencent签名REST，默认超时；禁止生产mock。
- [ ] tests/sms.test.mjs验证禁止生产mock、请求签名和参数、供应商失败、网络不确定结果。
- [ ] root集成outbox串行投递、唯一事件键、mock短信中心。

## Task 4: 整体验证与交付
- [ ] 独立审查权限、OTP、并发、UI/API字段，对发现的问题修复并复测。
- [ ] npm test与npm run check；尝试本机微信开发者工具编译，保存结果。
- [ ] README写启动、导入、演示账号、真实环境配置和未验证边界；保存本地提交，交付项目。
